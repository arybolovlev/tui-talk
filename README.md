# GatewayAPI talk TUI

A tiny Bubble Tea terminal user interface for the talk **An overheard conversation about GatewayAPI**.

## Run

```sh
go run .
```

Use the left and right arrow keys to move between pages. `h` and `l` work too. `q`, `Esc`, or `Ctrl-C` exits.
